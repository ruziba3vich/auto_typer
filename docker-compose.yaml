services:
  auto_typer:
    build: .
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - ./texts:/texts
    devices:
      - /dev/uinput:/dev/uinput
    privileged: true
